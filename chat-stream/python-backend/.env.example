# Server
PORT=8000

# Prefer a single URI (same style as node-postgres-backend)
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# Or discrete vars (used when DATABASE_URL is empty)
PGHOST=localhost
PGPORT=5432
PGUSER=
PGPASSWORD=
PGDATABASE=
